# Validation round three: the commit that answered VB2

**Status:** open

**Prefix:** VC2. Checked free before use: no document in `docs/reviews/` declares it, no `VC2-` id is in `docs/manual-tests/tests.md` or `findings.tsv`, and none is in `src/` or `test/`.

**Covers** `7d826926` on `autonomy-diagram-r0` ("Validation round 2: the exit asks once, and two tests that proved nothing"), read against the working tree at HEAD, which has no uncommitted change under `src/`, `test/` or `docs/` (`git diff HEAD --stat` is empty there). The commit claims five things: VB2-B1 (the duplicate layout-editor question removed from `TrainControlUI.WindowClosed`, and `regression.testEditorSurfaceRules.testLeavingCompletesBothHalvesOfADiscard` following the question into `everyOpenWindowMaySettle`), VB2-B2 (`core.testLayoutTiles.testAnyReplacedPictureGivesUpAnOutstandingFlash` made non-vacuous), VB2-B3 (`core.testMassAssignLengths.testALegThatCrossesItsOwnSquareTwiceIsCutAtIt`), VB2-C4 (`LayoutLabel`'s accessory timer clears `accessoryHighlight` only while it is still its own), and `testTheMaximumWalkWritesWhatIsTypedAndMovesOn` pressing OK instead of dispatching Enter.

**Read-only.** Nothing was run, built or edited; no test was executed; nothing under `src/`, `test/`, `cs2_sample_layout/` or the tracker was touched. Every "red" and "green" below is argued from the code, under the SOP's warning about simulating one's model of it; where a claim rests on timing, or on JDK behaviour I could not settle by reading, it says so.

The questions asked, from the brief: does each fix hold on every path and is anything claimed untrue; is the exit's order of questions still what OB-070 and WKV-B2 require, and can the now-public `everyOpenWindowMaySettle` be reached where a modal question would be wrong; is the changed surface rule still a guard, and what mutation slips past it; are the three changed or added tests deterministic, do they assert their preconditions, and would each catch its defect; which other tests dispatch keys and so depend on this desktop giving a window focus; and what in the day's records now disagrees with HEAD.

---

## A - wrong behaviour on the layout, or data silently lost

| id | status | where |
|---|---|---|
| - | - | none found |

Nothing reached A. The shapes looked for: a command sent wrongly (no change here touches a command path); data lost (VB2-B1's Discard-then-Save is gone - `openEditor.maySettleBeforeExit()` is now reachable exactly once per exit, VC2-D1); a lock or thread hazard (the only new field is a one-element array local to an EDT block, read by a Swing timer on the EDT, VC2-D5).

---

## B - incorrect results in specific configurations, a fix that guards nothing, or a test that guards nothing

| id | status | where |
|---|---|---|
| VC2-B1 | fixed - core.testCancelUndoesAutonomyEdits.testTheExitAsksOneQuestionPerEditor | The VB2-B1 fix has no guard: put the deleted block back above `TrainControlUI.java:19211` and `testEditorSurfaceRules.testLeavingCompletesBothHalvesOfADiscard` (`:2707-2733`) stays green, because `indexOf` finds the first `maySettleBeforeExit()` and nothing asserts there is only one. A runtime test that goes red on the duplicate was within reach of the suite as it stands |
| VC2-B2 | fixed - the rule names both windows | The rewritten rule cannot see the LAYOUT editor's question go missing: `bodyOf(ui, "public boolean everyOpenWindowMaySettle()").contains("maySettleBeforeExit()")` (`:2710-2713`) is satisfied by the route editor's call at `TrainControlUI.java:622` alone, and the runtime test `testTheExitAsksEveryOpenWindow` pins only the route editor. Delete `:615` and the whole battery is green while File > Exit with a dirty diagram editor discards without a word - OB-070's original defect |

### VC2-B1 - the exit asks once, and nothing would notice if it asked twice again

**Where.** `test/regression/testEditorSurfaceRules.java:2707-2733`. After the `.replace("everyOpenWindowMaySettle()", "maySettleBeforeExit()")` at `:2708`, the rule takes `exit.indexOf("maySettleBeforeExit()")` (`:2715`), asserts it is `>= 0` (`:2724`) and that `completeExitDiscard()` comes after it (`:2732`). Reinsert VB2-B1's block -

```java
if (openEditor != null && openEditor.isDisplayable() && !openEditor.maySettleBeforeExit())
{
    return;
}
```

- above `if (!everyOpenWindowMaySettle()) return;` at `TrainControlUI.java:19211`, and `settles` is the index of that first call, `completes2` is still after it, and every assertion holds. VB2-B1's own "Prove it" asked for "exactly one of `openEditor.maySettleBeforeExit()` / `everyOpenWindowMaySettle()` and not both"; the commit message says the rule "follows the question into the method and still pins the order", which is true, and says nothing about pinning it once - which is the defect being fixed. This is SVT-B1's and VB2-B2's shape for the third time in one day: a fix landed without a test seen red against it.

**A runtime test was available.** The suite already has every part: `testCancelUndoesAutonomyEdits.Answerer` (`:943-1000`) is a watcher thread that presses a named button on EVERY showing dialog it finds, polling every 50 ms, and records that it did (`answered`, a boolean - one field type away from a count); `opened()` (`:771-788`) builds a real `LayoutEditor` on that class's `ui` (leave out `setAutonomyMode` for track mode); `LayoutEditor.snapshotLayout()` (`:5535`, private, one reflection call) pushes `previousLayoutComponents` so that `canUndo()` (`:5495`) is true, which is what `settleUnsavedWork` (`:6071-6073`) reads in track mode; `TrainControlUI.openEditor` is a private field the tiles test's own pattern reaches (`testLayoutTiles.java:462-467` does it to `routeEditor`); and `everyOpenWindowMaySettle` is public. Set the editor as `openEditor`, start an `Answerer` on `layout.ui.switchDiscard`, call `ui.everyOpenWindowMaySettle()` on the EDT, stop the answerer and assert `answered() == 1`. At `9cdfb0c0` that reads 2 (the second call to `settleUnsavedWork` resets `settledByDiscarding` and asks again, `:6076-6090`); at HEAD it reads 1; and it is the test that would have caught VB2-B1 when it was made.

**Why B.** VB2 rated "a fix that guards nothing" B and this is that. The fix is right (VC2-D1) and nothing holds it in place; the rule's own javadoc (`:2651-2657`, "MUTATION: ... each fails one of these") reads as though the mutations it names were the only ones.

**Prove it.** By reading: no assertion in `:2707-2733` counts occurrences. By running, after the battery: reinsert the four lines above `:19211` and run `testEditorSurfaceRules` - green. **Fix.** Either the runtime test above, or in the rule count the occurrences of `maySettleBeforeExit()` in `exit` and assert exactly one - and then the rule's javadoc should say so.

### VC2-B2 - the layout editor's question at the exit is pinned by nothing

**Where.** `test/regression/testEditorSurfaceRules.java:2710-2713` and `test/core/testLayoutTiles.java:448-500`. `everyOpenWindowMaySettle` (`TrainControlUI.java:613-623`) asks two windows: `openEditor.maySettleBeforeExit()` at `:615` and `routeEditor.maySettleBeforeExit()` at `:622`. The new assertion requires the method body to contain `maySettleBeforeExit()` - which `:622` satisfies on its own. The runtime test drives only the route editor (`RouteEditorFrame.discardAnswerForTest`, `:480-498`); no test builds a `LayoutEditor` and sets it as `openEditor`. So delete `:615`, and: the source rule passes (the substring is still there), `testTheExitAsksEveryOpenWindow` passes (it never had a layout editor), and File > Exit with a dirty track editor goes straight to the trains dialog and `System.exit`, discarding the diagram without asking - the defect OB-070 was written for, and the one this rule's first paragraph (`:2640-2645`) describes.

**When it started.** Before `2a36ade7` the only `maySettleBeforeExit()` in `WindowClosed` was the layout editor's, so the old rule pinned it by accident. `2a36ade7` put the route editor's call beside it in `WindowClosed` (the old rule then accepted either), and `7d826926` moved the pin into `everyOpenWindowMaySettle` with a failure message about "any window" - the moment the rule was rewritten with both calls in view and still left the layout editor's unnamed. Recorded here because this commit is where the rule was reconsidered, not because it created the gap.

**Prove it.** By reading, as above. By running, after the battery: delete `:615` and run `testEditorSurfaceRules` and `testLayoutTiles` - both green. **Fix.** Require `openEditor.maySettleBeforeExit()` and `routeEditor.maySettleBeforeExit()` by those spellings in the method body (two `contains`, two messages naming the window), or write VC2-B1's runtime test, which covers this as its precondition - a dirty layout editor answered Discard is asked at all.

---

## C - low: comments that disagree with the code, a half-fix, and records not moved

| id | status | where |
|---|---|---|
| VC2-C1 | fixed - the hold is 2250 ms and the wait is asserted | `testLayoutTiles.java:317-318`: "BOUNDED WELL INSIDE THE FLASH'S OWN HOLD (5000 ms)" - `flashHighlight()` (`LayoutLabel.java:1130-1133`) holds `HIGHLIGHT_DURATION` = 2250 ms (`:61`); the 1500 ms poll is inside it by about 700 ms, not 3500, and the test asserts neither the elapsed time nor that the picture was replaced (`awaitIconChange`, `:394`, is still unused) |
| VC2-C2 | fixed - the javadocs say OK | `testMassAssignLengths.java:568`: the javadoc still says "Enter in the number box submits" over a body that presses OK (`:609`) and says why Enter was given up |
| VC2-C3 | fixed - the superseded timer is stopped | VB2-C4's fix shipped without the test its entry wrote out, and fixes the predicate only: the superseded timer is never stopped (`LayoutLabel.java:1034-1064`), so it still fires `setIcon(lastIcon)` at its own time over whatever is up - the second highlight's wash 500 ms early, or a flash started after `endAccessoryHighlight()` stopped only the CURRENT timer (`:1224`) |
| VC2-C4 | fixed - dispositions, and MT-462 gained the click and sensor steps | The records did not follow the fixes: the VB2 status tables (`2026-09-19-VB2-*.md:32-34`, `:92`) and `findings.tsv:2917-2923` still say open for B1, B2, B3 and C4; MT-462 (`tests.md:23254-23280`) still throws the turnout "from the keyboard or from the Central Station" and has no click or sensor step, while the automated test now covers the sensor path only |
| VC2-C5 | open - the arithmetic claim and the live-snapshot probe | The figure-of-eight test pins the cut and not the arithmetic both SVA-C2 and VB2-B3 asked for ("the pieces' total plus twice the crossing's"); VB2-B3's far-end off-by-one mutation also slips it (the second occurrence is at index 10 of 13, not the last intermediate square); and the probe over `test/layouts/live-snapshot` has now been deferred three times |
| VC2-C6 | open - two focus preconditions elsewhere, unverified | Two other tests assert, rather than skip on, a focus precondition that depends on this desktop: `testTheKeyMapReachesTheWholeWindow.focusOn` (`:330-341`) and `testTheWindowTakesTheKeyboard` `:447-452`. Whether `Window.getMostRecentFocusOwner()` follows `requestFocusInWindow()` in a window the desktop has not focused I could not settle by reading; if it does not, these are the walk test's failure in another class |

### VC2-C1 - the flash test's margin is 700 ms, and its comment says 3500

`test/core/testLayoutTiles.java:282-326`. The test is no longer vacuous (VC2-D3): a FEEDBACK square never takes the accessory branch, `updateImage(true)` (`LayoutLabel.java:1877-1889`) forces `setImage(true)`, the key is unchanged since the first draw so it is a cache hit straight to `setImageOnEDT` -> `invokeLater` -> `discardFlash()` at `:1007`, and with `discardFlash()` moved back inside the branch at `:1026` the flash lives its full hold and the poll gives up first. But the hold is 2250 ms - `flashHighlight()` at `:1130-1133` passes `HIGHLIGHT_DURATION`, `:61` - and the comment at `:317-318` says 5000, which was the OLD poll's length. The next author who reads "5000" and widens the poll to 3000 "well inside" makes the test vacuous again without touching the code under test. Two things would make the claim self-checking: record `System.currentTimeMillis()` before `flashHighlight()` and, once the poll sees the flash gone, assert the elapsed time is under 2250 (so a flash that timed out cannot pass, whatever the poll length); and assert the precondition VB2-B2's recipe asked for - that the picture WAS replaced - which `awaitIconChange(label, washed)` (`:394-404`) answers, since every replacement sets a new `ImageIcon` (`:996`). Then the fixture cannot drift to a square whose redraw is a no-op.

### VC2-C2 - the walk test's javadoc describes the gesture it gave up

`test/core/testMassAssignLengths.java:564-570`: "Asked of the real walk on the event thread, through the same prompt Mass Assign Lengths uses - so its title is the walk's own name, Enter in the number box submits, and Skip leaves the next station as it was." The body presses OK (`:603-609`) and the comment there explains why Enter was moved to `testTheNumberFieldHasFocusAndEnterSubmits`. One phrase: "OK submits" (Enter's claim is the other test's).

### VC2-C3 - the accessory timer's fix is half of VB2-C4, and untested

`src/org/traincontrol/gui/LayoutLabel.java:1026-1064`. The change is right as far as it goes: T1's restore now clears the field only while the field is T1 (`:1059`), so after a three-way's second drive `isAccessoryHighlightOutstanding()` stays true until T2 fires, and T2 clears itself (VC2-D5). What is left is what VB2-C4 named as the other half of the cost: T1 is never stopped. It still fires at its own 2250 ms and, `lastClicked` permitting, does `this.setIcon(lastIcon)` (`:1040-1055`) - taking T2's wash off 500 ms early (as before), and if a flash has started meanwhile, `endAccessoryHighlight()` (`:1220-1229`) stopped T2, the field's timer, and not T1, so T1 later puts the plain picture over the flash's wash. The picture is never wrong (`lastIcon` is the tile's current base picture and `flashRestore` still holds the right one); the cost is a flash cut short, which VB2-C4 already priced. `if (accessoryHighlight != null) accessoryHighlight.stop();` before `:1036` closes both halves, and then the `mine[0]` comparison is belt and braces rather than the fix.

And no test. VB2-C4 wrote the recipe - on a warmed signal label, `updateImage(true)` twice with 500 ms between, wait 2300 ms, assert `isAccessoryHighlightOutstanding()` - and the commit's `test/` diff touches `testLayoutTiles` only for the flash test. Revert `:1059` to `accessoryHighlight = null;` and the whole battery is green. The memory rule "every fix ships with a test, seen failing first" applies.

### VC2-C4 - the records were not moved

- `docs/reviews/2026-09-19-VB2-validation-round-two.md` is untracked (`git status`: `??`) and its head tables still read "open" for VB2-B1 (`:32`), B2 (`:33`), B3 (`:34`) and C4 (`:92`), the four things the commit fixes; the commit touches no document. `findings.tsv:2917-2923` says the same. By the SOP's "one status, one location", the one place a reader looks says the work is undone. Whoever moves them: B1 to "fixed, unguarded (VC2-B1)", B2 and B3 to "fixed" with VC2-C1 and VC2-C5 beside them, C4 to "half - the field, not the timer (VC2-C3)".
- MT-462 (`tests.md:23254-23280`) is unchanged: step 2 still throws the turnout "from the keyboard or from the Central Station", the two sources that took the accessory branch BEFORE SVB-B1, and there is no click-within-two-seconds step and no sensor step. The automated test now covers the sensor path (a FEEDBACK square) and nothing covers the click path; the manual test should carry the two the machine cannot, as VB2-B2 asked.
- MT-461's line at `tests.md:23244` ("its question comes first: answer that one, and the route editor's follows") is now true of the code (VC2-D1) - the one record the commit made right without touching it.

### VC2-C5 - the figure-of-eight test pins half of the claim

`test/core/testMassAssignLengths.java:1320-1372`. The fixture is right (VC2-D4) and the precondition is real. What it asserts is membership: the crossing is in `sharedSquaresALengthRuleReads()` and in no piece. SVA-C2's entry and VB2-B3's "Prove it" both asked for one more thing - "after assigning the pieces and the crossing, the leg's `getLength()` is the pieces' total plus twice the crossing's" - which is Adam's ruling ("count that length once in each direction") stated as a number. Without it, `GraphReducer.sumLength` counting each DISTINCT tile of a leg once - a plausible tidy-up - passes this test and `testACrossingIsCutOutOfEveryPieceAndCountsOnBothRoads` (`:734-776`, two legs with the crossing once each) and under-measures the figure-of-eight leg by one crossing. The recipe is the two-leg test's own (`:756-769`): assign 6 to every piece and 3 to the crossing, `rebuild()`, and assert `edge(key(1, 2), key(3, 4)).getLength()` against the sum - and assert the piece count while there (three, if the cut at a twice-crossed square works as it does at a switch; I did not read `stretchesNeedingALength` to confirm the count, so let the first run say).

Two smaller things. VB2-B3 named a second mutation - counting only `i + 1 < leg.size() - 1`, an off-by-one at the far end - and this fixture does not catch it: the leg is 13 squares, the second crossing is at index 10, and `11 < 12` still counts it. Moving the second sensor to 3,3 (dropping the straight) puts the crossing last-but-one and makes that mutation red too. And the probe over `test/layouts/live-snapshot` that SVA-C2 asked for, so MT-459's "may be fewer" can be a number, is now three rounds deferred.

### VC2-C6 - the other tests that need the desktop's focus

Answering the brief's question 5. Every key dispatch in `test/` was read:

- `testMassAssignLengths.testTheNumberFieldHasFocusAndEnterSubmits` (`:427-519`) is the one remaining Enter dispatch, and it is sound: it polls up to 5 s for `first.isFocused()` with a `JTextField` as owner and SKIPS otherwise (`:462-470`). The mechanism the commit names is right - `JTextField.NotifyAction.actionPerformed` calls `getFocusedComponent()`, the last `JTextComponent` to gain focus in the app context, and ignores the event's source - so a dispatched Enter reaches `postActionEvent` only once the field has gained focus, and that is exactly what the poll waits for.
- `testEscapeClosesTheEditor.escape` (`:344-350`) dispatches at the frame and is handled by `formKeyPressed`'s own listener; its comment says why no focus is needed. Sound.
- `testTheWindowTakesTheKeyboard.press` (`:217-227`) dispatches to the component itself. Sound. But `:447-452` does `elsewhere.requestFocusInWindow()` and then `assertSame(up.ui.getMostRecentFocusOwner(), elsewhere, "precondition: ...")`.
- `testTheKeyMapReachesTheWholeWindow.pressForReal` (`:298-322`) posts to the system queue and SKIPS when the OS focus owner is not the target - sound - but it calls `focusOn` first (`:330-341`), which `requestFocusInWindow()`s and then `assertSame(ui.getMostRecentFocusOwner(), on, ...)`.
- `testTheTailCanBeGivenInTheEditor`, `testAPastedTrainFacesTheWayTheOperatorChose`, `testTheGreyAppearsAtIdleToo` invoke a gesture method by reflection with a key code; no event, no focus.

So the two candidates are the asserted `getMostRecentFocusOwner()` preconditions. Whether they fail on the desktop state that broke the walk test turns on whether `requestFocusInWindow()` on a component in a window the desktop has not focused updates the window's most-recent-focus-owner record; `Component.requestFocusHelper` returns false when the peer refuses, and I could not settle from reading whether the record is written before or after that refusal. Recorded as a C to check against the battery's history for those two classes rather than as a claim; the fix, if they do, is the shape `pressForReal` already has - a `SkipException` naming what holds the keyboard - in place of the assert.

---

## D - not defects: what was checked and came back clean

| id | what |
|---|---|
| VC2-D1 | **VB2-B1's fix holds on every path, and the order is what OB-070 and WKV-B2 require.** `WindowClosed` (`TrainControlUI.java:19195-19284`) now does, in order: `everyOpenWindowMaySettle()` (`:19211`); the trains dialog, only while autonomy runs (`:19225-19246`, anything but Yes returns); `model.saveState` and `saveState` (`:19250-19251`); `openEditor.completeExitDiscard()` then `dispose()` (`:19277-19282`); `System.exit`. `everyOpenWindowMaySettle` (`:613-623`) asks the layout editor first and short-circuits on its Cancel before the route editor is asked, then the route editor. `LayoutEditor.maySettleBeforeExit` (`:5994-6013`) -> `settleUnsavedWork` (`:6071-6136`) is therefore entered once per exit: a track-mode Discard sets `settledByDiscarding` and defers to `completeExitDiscard` (`:6045-6048`), which runs only after the trains dialog and the save; an autonomy-mode Discard restores inside `settleUnsavedWork` and rebuilds the running layout at `:6011` before the exit's save, which is WKV-B2, unchanged. A Save no longer meets a second question over an undo stack it did not clear. Both doors into the handler call it once - the window listener at `:13291` and File > Exit at `:23538` (`WindowClosed(null)`). The UIX-B1 paragraph deleted from the handler survives on `everyOpenWindowMaySettle` (`:617-621`), so the reasoning was moved, not lost |
| VC2-D2 | **`everyOpenWindowMaySettle` is reached from nowhere it should not be.** A grep for `everyOpenWindowMaySettle` over `src/` and `test/` finds the declaration, the call at `:19211`, and `testLayoutTiles.testTheExitAsksEveryOpenWindow` (`:461`, `:481`, `:492`, `:498`). No `View` twin, no other window's close path. The test calls it from the test thread with `discardAnswerForTest` set, so no dialog; its precondition would hang the battery on a dirty `openEditor`, and `testLayoutTiles` builds no `LayoutEditor` (a grep for `openEditor` or `LayoutEditor` finds nothing in the class). The only cost of `public` is the one VB2-D8 recorded: a modal door now callable from anywhere, with a test hook for one of its two windows |
| VC2-D3 | **VB2-B2's test now goes red under the mutation it names.** The fixture is a bare FEEDBACK (`testLayoutTiles.java:284-285`), which is neither a signal nor a switch and so fails the branch condition at `:1026`; `updateImage(true)` (`:1877-1889`) calls `setImage(true)` unconditionally; `getImageKey` is unchanged since the first draw so `imageCache.get(key) != null` (`:896`) and it goes straight to `setImageOnEDT` (`:952`) -> `invokeLater` -> `discardFlash()` (`:1007`) before `setIcon` (`:1009`). With `discardFlash()` back inside the branch, nothing stops the 2250 ms flash timer and the 1500 ms poll (`:320-325`) ends first, so `assertFalse(isFlashOutstanding())` fails. The precondition `assertFalse(isAccessoryHighlightOutstanding())` (`:313-314`) is trivially true of a FEEDBACK and is the right thing to assert: it is the fixture's contract, and a future author who swaps in a switch is told what path they have left. Deterministic apart from the ~700 ms margin in VC2-C1; the discard is one `invokeLater` behind a cache hit, with no decode |
| VC2-D4 | **VB2-B3's fixture is a figure of eight, and the test asserts it.** `TilePorts` (`:26-27`, `:248-253`): CURVE is E-S at orientation 0 and orientation `o` applies `4 - o` clockwise quarter turns, so o=2 is W-N (the 5,2 tile), o=3 is S-W (5,0), o=0 is E-S (3,0) - the sides the fixture's comment (`testMassAssignLengths.java:1330`) claims; STRAIGHT and FEEDBACK are E-W at 0 and N-S at 1; CROSSING carries both roads. The walk from the sensor at 1,2 enters the crossing from the west and later from the north - two different tile-and-entry-side keys in `GraphReducer.continueWalk`, so one leg of 13 squares to the sensor at 3,4. The precondition loop (`:1346-1360`) counts occurrences in `getPath()` and fails, not passes, on a fixture whose loop did not join. With `onceEach` restored (each leg contributing each square once) the crossing is crossed by one leg, `crossedBy` reads 1, the `> 1` test at `:3236` excludes it, and the first assertion (`:1362`) is red. The residue is VC2-C5 |
| VC2-D5 | **VB2-C4's field logic is right, and no wait can hang on it.** `mine` (`LayoutLabel.java:1034`) is a final one-element array captured by the timer's lambda; both the write at `:1062` and the compare at `:1059` are on the EDT. T2 replaces the field and clears itself when it fires; `endAccessoryHighlight` (`:1220-1229`) stops and clears whichever timer the field holds. Every path ends with the field null, so `warmedSignalLabel`'s wait for the warm-up highlight to end (`testLayoutTiles.java:366-376`) and `awaitHighlight` are unaffected - those tests raise one highlight at a time. `endAccessoryHighlight`'s stop is on the field's timer only (VC2-C3). The surface rule's new anchor `new javax.swing.Timer(HIGHLIGHT_DURATION` (`testEditorSurfaceRules.java:1462`) is unique to the accessory branch - the flash's timer takes `holdMs` (`:1174`) - so the rule still finds the thing it means |
| VC2-D6 | **Pressing OK is the right fix, and it needs no focus.** `answer(first, OK)` (`testMassAssignLengths.java:1050-1062`) calls `pane.setValue(option)` on the EDT with the very object `askForWholeLength` put in `answers` (`AutonomyEditorPanel.java:9616`); `JOptionPane.createDialog`'s own listener closes the dialog on that value change; `setVisible(true)` returns (`:9638`), `dialogAnswer` reads OK (`:9643`), and `field.getText()` is the "7" the test set (`:606`). Skip and Cancel have been answered this way in green tests all week. `awaitPrompt`'s new failure message (`:1003-1017`) lists what IS showing, which is the diagnosis VB2 could not make from "no prompt appeared". The commit's mechanism claim is right (VC2-C6, first bullet) |
| VC2-D7 | **The surface rule still pins the order, and cannot be satisfied by a comment or a rename.** `bodyOf` (`testEditorSurfaceRules.java`) returns `""` for a declaration it cannot find, so renaming `everyOpenWindowMaySettle` makes `contains` false and the assertion at `:2710` fail; `withoutComments` strips both comment kinds before the search, so the paragraph at `TrainControlUI.java:19207-19210` that names `everyOpenWindowMaySettle` cannot stand in for the call. Moving `completeExitDiscard()` above `:19211`, or deleting it, each fails one of `:2728-2733`. What it cannot see is VC2-B1 and VC2-B2 |
| VC2-D8 | **Nothing else in the tree moved.** `git diff HEAD --stat` over `src/`, `test/` and `docs/` is empty; the untracked files are the five review documents, `random_test_layout/` and a `Main_bak.json` under the sample layout, none of which the commit claims. The `hs_err_pid*.log` files at the root are dated 2026-08-29 and are not this battery's. `VC2` is free everywhere the SOP asks |
| VC2-D9 | **The SV tables the commit's fixes bear on still say true things.** SVA-C2 "fixed - a leg crossing its own square twice counts" (`SVA-*.md:44`) is now backed by a test; SVB-B1 "every replacement gives up a flash" (`SVB-*.md:34`) is now backed by a test that can fail; SVB-C1 "the timer nulls its field" (`:68`) is still what the timer does, under one more condition. The still-open VB2 C items this commit did not claim - VB2-C1 (the `MarklinRoute` orphan), VB2-C2 (`TrainControlUI.java:629` still names `testTheExitKnowsTheRouteEditorHasUnsavedWork`), VB2-C3, VB2-C5, VB2-C6 - are untouched and correctly open; they are not repeated here |

---

## What this pass did not cover

- Nothing was run. VC2-D3's red-under-mutation and VC2-D4's geometry are argued from `updateImage`, `setImage`, `TilePorts` and `continueWalk`, not observed; the battery that was running while this was written will say whether the three tests are green at HEAD, and only a mutation run says whether they are red without the fixes.
- Whether `Window.getMostRecentFocusOwner()` follows a refused `requestFocusInWindow()` (VC2-C6). The JDK was not read; the two classes' battery history is the cheaper check.
- `stretchesNeedingALength` on a leg that holds one square twice - how many pieces it makes - which VC2-C5's recipe would need to assert a count.
- `LayoutDiagram.addComponent`'s argument order beyond what the sibling fixtures use in the same positions.
- The live-snapshot count VB2-B3 and SVA-C2 asked for, deferred again.
